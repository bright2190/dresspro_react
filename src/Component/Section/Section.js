import React from 'react'
import "./Section.css"
import { Policy } from '../Policy/Policy'

const Section = (props) => {
  return (
    <div><div>Section</div>
    <div>{props.cloth_name}</div>
    </div>

  )
}

export default Section